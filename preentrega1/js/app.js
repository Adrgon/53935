// variables
// constantes
// condicional
// ciclos
// funciones


/* let cantidadAlumnos = Number(prompt("Cantidad de alumnos"))

for(let i=0; i<cantidadAlumnos; i+=1){
    let alumno = prompt("Ingresar Nombre")
    alert(alumno)
} */

let dado = (parseInt(Math.random()*6)+1)


console.log(dado);
